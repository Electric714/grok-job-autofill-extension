Full project for building unsigned Safari App Extension IPA via GitHub Actions only.

See project.yml and .github/workflows/build.yml

This extension auto-fills job applications using answers from Grok's job-application-assistant skill.

Features: floating button, Workday shadow DOM support, multi-page auto-advance, dark mode popup, one-tap JSON from skill.